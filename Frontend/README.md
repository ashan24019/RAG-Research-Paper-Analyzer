# Frontend (React)

Minimal Vite + React scaffold. To run locally:

1. Install dependencies:

```bash
cd frontend
npm install
```

2. Start dev server:

```bash
npm run dev -- --port 3000
```

The simple UI expects the backend FastAPI server at `http://127.0.0.1:8000` (see `frontend/src/App.jsx`).
